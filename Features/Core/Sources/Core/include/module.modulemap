module Core [system] {
    umbrella header "Core.h"
    export *
    module * { export * }

    link framework "Cgit2"

    explicit module Swift {
        header "Core-Swift.h"
        requires objc
    }
}
