import SwiftUI

struct VisualizationView: View {
    var body: some View {
        VStack {
            Text("Visualization")
                .font(.largeTitle)
            Text("This is where the visualization features will be implemented.")
                .font(.subheadline)
        }
    }
}

struct VisualizationView_Previews: PreviewProvider {
    static var previews: some View {
        VisualizationView()
    }
}
